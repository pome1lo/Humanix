<Window x:Class="WPF.Desktop.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WPF.Desktop.UI.ViewModels"
        xmlns:local="clr-namespace:WPF.Desktop.UI.Views"
        mc:Ignorable="d"
        Name="LoginView"
        Title="Login" Height="450" Width="450">
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <Button Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=LoginView}">Login</Button>
    </Grid>
</Window>
