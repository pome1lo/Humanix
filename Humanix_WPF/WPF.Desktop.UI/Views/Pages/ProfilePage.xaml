<Page x:Class="WPF.Desktop.UI.Views.Pages.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:WPF.Desktop.UI.ViewModels"
      xmlns:local="clr-namespace:WPF.Desktop.UI.Views.Pages"
      mc:Ignorable="d" 
      Background="Transparent"
      Height="396"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfilePage">
    <Page.DataContext>
        <vm:ProfileViewModel/>
    </Page.DataContext>
    <Border CornerRadius="12">
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <TextBlock FontSize="50" Text="PROFILE"/>
                <TextBlock FontSize="20" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock FontSize="20" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock FontSize="20" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock FontSize="20" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel>
                
            </StackPanel>
        </StackPanel>
    </Border>
</Page>
